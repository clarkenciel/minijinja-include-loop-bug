# minimal reproduction of minijinja failure with mulitple `include`s

To run the `minijina` version:

```shell
cargo run
```

To run the comparison python version:

```shell
pip install -r requirements.txt
python jinja_test.py
```
